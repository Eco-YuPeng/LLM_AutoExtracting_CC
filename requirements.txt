# Core dependencies for cover crop literature extraction

# PDF handling
pymupdf>=1.24        # fitz — PDF opening, metadata, page-to-image rendering
pymupdf4llm>=0.0.5    # PDF -> markdown conversion preserving heading structure

# Config / schema
pyyaml>=6.0

# Data output
pandas>=2.0
openpyxl>=3.1         # Excel export, if the user wants .xlsx instead of .csv

# External validation APIs (Task 11) — network calls only, no local install needed
requests>=2.28

# Testing
pytest>=7.0

# NOTE: LLM calls (classify_response_type, extract_narrow_llm,
# extract_vision_llm) are stubs in src/literature_extractor.py — add
# your model provider's SDK here once you wire them (e.g. anthropic>=0.40).
