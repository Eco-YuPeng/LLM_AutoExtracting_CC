# Workflows

Each extraction run gets its own folder here:

```
workflows/<project_name>/
  <script>.py       # your workflow script (see AGENTS.md's Required Script Header)
  output/           # generated table(s), append-only
```

Do not edit `examples/book4_reference/` — that's the read-only reference.
Copy its pattern into a new folder here for your own run.
